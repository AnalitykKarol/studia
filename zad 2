#include <iostream>
#include <string>
using namespace std;

int main() {
    string name;
    cout << "Insert name: ";
    cin >> name;

    int ns = name.size();

        if (name.at(nameSize - 1) == 'a') {
            cout << "kobieta ";
        } else {
            cout << "mezczyzna ";
        }
    for(int i = ns - 1; i>=0;i--){
    cout<<name.at(i);
    }

    return 0;
}
